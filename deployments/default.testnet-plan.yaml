---
id: 0
name: Testnet deployment - legion-registry
network: testnet
stacks-node: "https://api.testnet.hiro.so"
bitcoin-node: "http://blockstack:blockstacksystem@bitcoind.testnet.stacks.co:18332"
plan:
  batches:
    # Deploy ONLY legion-registry. The other three contracts (treasury/gov/fees)
    # are already live on testnet under STBEMQQ... and are NOT redeployed here.
    # Deployer = legion-agent-01 (STXGASYJ...), which becomes the registry Admin.
    - id: 0
      transactions:
        - contract-publish:
            contract-name: legion-registry
            expected-sender: STXGASYJR80W8RWNM7R4ENRJAPR75Y5W57J57V0J
            cost: 350000
            path: contracts/legion-registry.clar
            anchor-block-only: true
            clarity-version: 3
      epoch: "3.0"
